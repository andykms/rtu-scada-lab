В данной папке лежат типы данных для блоков
    
    
`internal` - типы данных для блоков, которые НЕ взаимодействуют с сетью

`network-blocks` - типы данных для блоков, которые взаимодействуют с сетью

`base-block.type.ts` - общий тип блока, все типы блоков расширяются от него